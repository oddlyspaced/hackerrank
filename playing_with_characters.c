// https://www.hackerrank.com/challenges/playing-with-characters/problem
#include<stdio.h>
int main(){
    char ch[50];
    char s[50];
    char sen[50];
    gets(ch);
    gets(s);
    gets(sen);
    puts(ch);
    puts(s);
    puts(sen);
}